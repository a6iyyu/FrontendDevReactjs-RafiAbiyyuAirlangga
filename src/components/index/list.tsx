import React, { useEffect } from "react";

export const DaftarRestoran: React.FC = () => {
  useEffect(() => {
    (async () => {
      try {
        
      } catch (e) {
        console.error(e);
      }
    })();
  }, []);

  return (
    <main className="">
      <section></section>
    </main>
  );
};